services:
  auth:
    build:
      dockerfile: ./app/Dockerfile
    ports:
      - 9702:9702
    networks:
      - gateway
      - internal
  
  
  dummy:
    build: 
      dockerfile: ./dummyServer/Dockerfile
    ports:
      - 9703:9703


networks:
  gateway:
    external: true
    name: auth
  
  internal:
    external: false
      